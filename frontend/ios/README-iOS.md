# iOS Build Cleanup and Workspace Guide

This project uses Expo/React Native with CocoaPods. If you see errors like "Module map file ... not found" for Expo/React Native pods, follow these steps.

## One-command cleanup

Run the helper script:

```bash
bash scripts/ios-clean.sh
