/**
 * Variance display components (canonical reconciliation surface).
 *
 * These components consume {@link VarianceViewModel}s produced by the adapter
 * layer in `@/viewModels`. They never recompute authoritative quantities.
 */
export { VariancePanel } from "./VariancePanel";
export type { VariancePanelProps } from "./VariancePanel";
