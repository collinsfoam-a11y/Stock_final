# Post-Fix Verification Audit

| Issue ID | Status | Evidence Location | Verification Result | Remaining Risk | Required Test |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **AUTH-01** | FIXED | `backend/api/session_management_api.py:1192,1311` | Replaced `!= "supervisor"` checks with `not in ("supervisor", "admin")`. | None | Role-based RBAC unit test for admin vs supervisor. |
| **AUTH-02** | FIXED | `backend/api/sync_batch_api.py:278-280` | Added `if is_finalized and str(user_role).lower() not in {"supervisor", "admin"}: is_finalized = False`. | None | Test submitting batch with `status=finalized` as staff role. |
| **PERF-01** | FIXED | `backend/api/session_management_api.py:702-709` | Refactored array `$push` to `$group` using `_id: "$status"` and `count: {"$sum": 1}`. | None | Test analytics payload scale over 16MB document size equivalent. |
| **PERF-02** | NEEDS TEST EVIDENCE | `backend/auth/dependencies.py` | Caching dictionary added with TTL, but no explicit tests prove the cache prevents database queries during high concurrency. | Cache drift or memory leaks if TTL is flawed. | Integration test asserting `get_user_by_username` DB hit count is 1 for N requests. |
| **PERF-03** | FIXED | `backend/api/sync_batch_api.py:462-466` | Pre-fetches idempotency keys using a single `$in` query before validating records sequentially. | None | Verify duplicate batch submission hits the DB exactly once for idempotency check. |
| **PERF-04** | FIXED | `backend/api/session_management_api.py` | JSON serialization wrapped in `asyncio.to_thread` to prevent event loop blocking. | None | Benchmark async throughput under high concurrency. |
| **PERF-05** | FIXED | `backend/services/projection_service.py:624` | `N+1` `find_one` loops were replaced by a native `items_snapshot.aggregate(pipeline)` operation. | None | Test dashboard rendering speed with 10k items. |
| **PERF-06** | NOT FIXED | `backend/services/projection_service.py:517-528` | Still uses an `async for row in self.db.verified_items_projection.find(...)` loop to count `verified_items += 1` in Python memory. | Fatal OOM crash / Event Loop Hang when processing huge datasets. | Needs a test that simulates 1M rows and asserts memory stays flat via `$count` or `$group`. |
| **DATA-01** | NOT FIXED | `backend/services/projection_write_service.py:602` | Projections still use `ReplaceOne` after calculating aggregates in Python (`financial["total_stock_value"] += ...`) instead of atomic MongoDB operators (`$inc`). | Data corruption/race conditions during concurrent syncs from multiple offline devices. | Integration test firing concurrent sync requests and asserting totals match exactly. |
| **PERF-07** | PARTIALLY FIXED | `frontend/src/db/localDb.ts:118-132` | Refactored from `db.runAsync` to `db.prepareAsync`, but still loops over `statement.executeAsync` crossing the JS-Native bridge `N` times instead of a native batch query. | ANR on 10k+ rows due to bridge congestion. | Device bridge profiling during large database seed. |
| **PERF-08** | NOT FIXED | `frontend/src/db/localDb.ts:241-245` | Restricts barcode to `prefixQuery`, but continues to use `OR name LIKE ?` with `%query%`, guaranteeing a full table scan and defeating B-Tree indexes. | UI lockup during typing. | EXPLAIN QUERY PLAN test to assert indexes are used. |
| **PERF-09** | FIXED | `frontend/src/db/localDb.ts:301-303` | Combined queries into a single `SELECT COUNT(*) as total, SUM(CASE WHEN verified = 1 THEN 1 ELSE 0 END) as verified`. | None | None |
| **UI-01** | NOT FIXED | `frontend/src/components/admin/dashboard/DashboardPanels.tsx:52-57` | Props still strictly use TypeScript evasion (`issues: any[]; servicesStatus: any; systemStats: any;`). | Silent runtime crashes on API payload changes. | Static analysis typescript check. |
| **PERF-10** | FIXED | `backend/api/erp_api.py` | Heavy synchronous pyodbc calls wrapped in `asyncio.to_thread`. | None | Test FastAPI throughput during slow ERP SQL Server queries. |
| **PERF-11** | FIXED | `backend/services/count_line_write_service.py:592-620` | `_compute_session_totals` uses a `$group` aggregation pipeline entirely inside the database instead of scanning all count lines in memory. | None | Performance benchmark of count submission scaling. |
| **DATA-02** | NOT FIXED | `backend/services/count_line_write_service.py:1113,1252,1658` | `cursor.to_list(length=5000)` remains, truncating the event log pre-images if bulk updates exceed 5k lines. | Event Log mirrors will be silently corrupted and irrecoverable. | Test bulk update over 5001 lines and assert event log coverage matches line count. |
| **PERF-12** | NOT FIXED | `backend/services/projection_write_service.py:600-612` | `_rebuild_item_projections` loops through `active_lines` in python and builds arrays of `ReplaceOne` updates instead of `$merge` pipelines. | Massive database I/O spikes and CPU choke on every scan. | Profiling writes per second. |
| **PERF-13** | PARTIALLY FIXED| `backend/services/validation_service.py:516-519` | Added `$match: {"archived": {"$ne": True}}` to restrict the pipeline, but this may still scan majority of data if archiving is rare. | High memory use for old sessions if not actively archived. | Assert EXPLAIN shows index utilization. |
| **PERF-14** | PARTIALLY FIXED| `backend/services/validation_service.py:537-547` | Added `archived: ne True` but missing partial indexes in `backend/db/indexes.py` means `{"location_id": None}` forces a full collection scan anyway. | Event loop stall on large collections without sparse index. | Test EXPLAIN plan for `$exists` or `null` checks. |
| **LOGIC-01**| FIXED | `backend/services/sync_conflicts_service.py:535-538` | `auto_resolve_simple_conflicts` bypasses the truncated `get_conflicts(limit=100)` and directly loops over a cursor `find({"status": "pending"})` with no limit. | None | Test resolution speed with 5,000 conflicts. |
