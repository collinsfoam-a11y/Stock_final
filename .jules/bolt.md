## 2024-05-24 - [List Rendering Optimization in React Native]
**Learning:** React Native's standard `FlatList` can suffer significant frame drops and memory issues when rendering long, complex items or paginated lists with infinite scrolling (like search results). The `VirtualList` component (which wraps `@shopify/flash-list`) is vastly superior for these use cases but requires a precisely calculated `estimatedItemSize` to function optimally.
**Action:** When working with potentially long lists in this codebase (especially in search or data tables), always prefer `VirtualList` over `FlatList`. Ensure you calculate an accurate `estimatedItemSize` by inspecting the item's layout and styles (padding, margins, font sizes) rather than guessing.## 2024-05-24 - [Jest Test Isolation in Large Suites]
**Learning:** Running the full frontend test suite (`pnpm test`) can take over 50 seconds and may fail due to unrelated brittle tests (like timeout errors in `settings.save.test.tsx`). This can block verifying targeted optimizations if we only rely on the full suite.
**Action:** When making isolated component optimizations, use targeted test runs `pnpm test -- ComponentName` to quickly verify the optimization didn't break functionality, while ignoring unrelated flaky tests.
