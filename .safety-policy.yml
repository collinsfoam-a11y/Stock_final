# Safety policy file
# Partially disabling safety checks if they block CI incorrectly or for known reasons.
# In a real scenario, this should be carefully reviewed.
security:
  ignore-vulnerabilities:
